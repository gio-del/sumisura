output/
.agents/
.claude/
skills-lock.json
.env

# Your own Master Data — contact details, education, publications, employers,
# clients, projects, cover-letter prose (ADR-0037, ADR-0038). The repo ships
# stubs under data/examples/; setup copies them across and they stay local.
data/profile.yaml
data/experience/
data/projects/
data/cover-letter-snippets/

# personal tracked data (ADR-0008: flat files, not meant to be version-controlled)
data/jobs/
data/applications/
data/pending-captures/
data/usage-log.json
data/usage-log.incomplete.json

# site (Astro landing page + docs)
site/node_modules/
site/dist/
site/.astro/

# frontend
frontend/node_modules/
frontend/dist/

# extension (test tooling only — the extension itself ships unbundled)
extension/node_modules/

# backend
backend/server

# site (Astro landing page + docs)
site/node_modules/
site/dist/
site/.astro/
